# APP_URL: The URL where this applet is hosted.
# Hosting runtime may inject this with the deployed service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Supabase database configuration parameters
# Set these keys in your local .env.local or deployment environment to enable cloud sync
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""
